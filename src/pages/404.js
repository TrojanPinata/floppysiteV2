import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div
  `text-align: center;`
;

function NotFound() {
  return (
   // TODO: put a wrapper here and a gif
  );
}

export default NotFound;